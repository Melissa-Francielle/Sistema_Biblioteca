package util;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;

import java.sql.Connection;

public class DaoJDBCTest {
 /* 
    @Test
    public void testInserir() {
      
        Objeto obj = new Objeto();
        obj.setMetodo("Valor1");
        obj.setMetodo("Valor2");
        
        Dao<Objeto> dao = new Dao<>(Objeto.class);
        dao.inserir(obj);

        // Adicionar verificações apropriadas após a inserção
    }
       */
/*
    @Test
    public void testConexao() {
        try (Connection connection = DatabaseConnection.getConnection()) {
            assertNotNull(connection, "A conexão não foi estabelecida.");
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
    */
}
